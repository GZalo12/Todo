// hoja de rutas
// importamos lo que necesitamos
import { Todo } from "./todo.class";
import { TodoList } from './todo-list-class';

// exportamos las palabras claves para que se reeusen en otros archivos
export {
  Todo,
  TodoList,
}